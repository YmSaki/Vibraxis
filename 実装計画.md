# Vibraxis 本体 実装計画（機能仕様）

目標・品質基準・スケジュールは `提出計画.md` を参照。本書は「何をどう作るか」だけを扱う。
analyze-tool（Python解析CLI）は別枠で確定済みのため対象外。本体は `catalog.json` と `tracks/` を読むだけ。

## 0. 全体構成

```text
Vibraxis/
├─ frontend/   # React + Vite + TS — UI・再生エンジン
├─ server/     # Node + TS + Hono — 配信・GPT-5.6プロキシ
├─ shared/     # 型定義・スコアリング（純関数）— 両側から import
├─ analyze-tool/  # (別枠) Python CLI
├─ tracks/     # 音源 mp3
└─ data/       # catalog.json / analysis/*.json
```

npm workspaces のモノレポ。設計原則：

- **サーバーはステートレス**。セッション状態（現在曲・再生履歴・直近の意図）はフロントが保持し、APIリクエストに毎回載せる。→ 将来のCloudflare Workers移行がデプロイ作業だけになる
- **選曲は2段パイプライン**: ①決定的スコアリング（shared、純関数）で候補を絞る → ②GPT-5.6がtop-Kから選んで理由を書く。LLMにカタログ全体を自由に選ばせない
- 曲アクセスは `MusicSource` インターフェース経由（今回は `LocalFolderSource` のみ実装）

## 1. 共有型定義（shared/src/types.ts）

```ts
type Track = {
  id: string
  title: string
  artist: string
  file: string          // tracks/ 内のファイル名
  durationSeconds: number
  bpm: number
  key: string           // 例 "A minor"
  camelot: string       // 例 "8A"
  energy: number        // 0..1
  genre: string
  moods: string[]
  beats?: number[]      // ビート時刻(秒)。ミックス頭合わせに使用
  license: string
  sourceUrl: string
}

type DJIntent = {
  energyTarget: number            // 0..1
  energyDirection: "up" | "down" | "hold"
  moods: string[]
  genres: string[]
  explicitTrackRequest?: string   // "play X by Y" の生テキスト
  comment: string                 // DJがユーザーに返す一言（MC的な相槌）
}

type ScoredCandidate = {
  track: Track
  total: number
  breakdown: { bpm: number; key: number; energy: number; tags: number; repeatPenalty: number }
}

type SetPlanItem = {
  trackId: string
  reason: string                  // GPTによる選曲理由（UI表示用）
  transition: { crossfadeSeconds: number }
}

type SessionContext = {           // フロントが保持しAPIに毎回送る
  currentTrackId: string | null
  positionSeconds: number
  playedTrackIds: string[]        // 新しい順
  lastIntent: DJIntent | null
}
```

## 2. サーバー（server/ — Hono）

| エンドポイント | 内容 |
|---|---|
| `GET /api/catalog` | `catalog.json` を返す |
| `GET /tracks/:file` | mp3配信（Range対応不要、fetch+decodeで全量読むため） |
| `POST /api/intent` | 下記のDJ思考パイプライン |
| `GET /*` | ビルド済みフロントの静的配信 |

### POST /api/intent — DJ思考パイプライン

リクエスト: `{ message: string | null, context: SessionContext }`

1. **意図解釈**（GPT呼び出し①）: `message` + `context.lastIntent` → `DJIntent`（Structured Outputs, JSONスキーマ強制）。`message` が `null`（自動継続時）はGPTを呼ばず `lastIntent`（無ければ energyDirection:"hold" のデフォルト）を使う
2. **候補生成**（sharedの純関数）: カタログ全曲をスコアリング → top-5。`explicitTrackRequest` がある場合はカタログとのマッチング（タイトル・アーティストの部分一致）を先に試み、ヒットしたら候補の先頭に固定
3. **選曲＋説明**（GPT呼び出し②）: `{intent, currentTrack, top5(+breakdown)}` → `{ selectedTrackId, explanation, plan: SetPlanItem[] }`（3〜5曲先のプランまで一括生成）

レスポンス: `{ intent: DJIntent, selection: SetPlanItem, plan: SetPlanItem[] }`

- GPT呼び出しは **`LLMProvider` インターフェース**経由（`parseIntent()` / `selectAndPlan()` の2メソッドだけ）:
  - **`OpenAIProvider`（デフォルト・審査用）**: 直接API、`model: "gpt-5.6"`、Structured Outputs。キーは `OPENAI_API_KEY`（未設定なら起動時に分かりやすく警告）。GPT-5.6使用の審査要件をコードで明示する主経路
  - **`CodexProvider`（polish枠）**: Codex SDK（TS）＋ChatGPTプランのサインイン認証。**APIキー不要で動く**。`codex exec --output-schema` 相当の構造化出力を使う。レイテンシは直接APIより悪い前提。README的には「Codexで作って、Codexの上でも動く」ネタ
  - 選択は環境変数 `LLM_PROVIDER=openai|codex`（デフォルト openai、キーが無くCodexサインインを検出できたら codex に自動フォールバックしてもよい）
- GPT失敗時 / どちらのProviderも使えない時: **フォールバックでスコア1位を機械選曲**（`selection.reason = "(auto-picked: LLM unavailable)"`）→ 音は止まらない。デモと審査員体験の保険

### MusicSource インターフェース

```ts
interface MusicSource {
  getCatalog(): Promise<Track[]>
  getAudioUrl(trackId: string): string   // 今回は "/tracks/<file>" を返すだけ
}
```

`LocalFolderSource` のみ実装。ストリーミング接続はFuture Work（READMEに接続面として明記）。

## 3. スコアリング（shared/src/scoring.ts — 純関数＋vitest）

```
total = 0.35·bpmScore + 0.25·keyScore + 0.25·energyScore + 0.15·tagScore − repeatPenalty
```

- **bpmScore**: `1 − min(|Δbpm| / 8, 1)`。倍/半テンポ互換も考慮（`|b·2 − a|`, `|b/2 − a|` の最小値を採用）
- **keyScore**（Camelot Wheel）: 完全一致 1.0 / 同番号でモード違い（8A↔8B） 0.8 / ±1番号で同モード 0.8 / それ以外 0.2
- **energyScore**: `1 − |candidate.energy − intent.energyTarget|`。`energyDirection` が up なのに現在曲よりenergyが下がる候補は ×0.5
- **tagScore**: intentの moods/genres と候補のタグの重なり率
- **repeatPenalty**: 直近5曲は −1.0（実質除外）、6〜10曲前は −0.3
- 重みは定数オブジェクト1箇所にまとめ、調整可能にする
- **vitestで単体テスト**: Camelot表・倍テンポ・repeatPenaltyの境界を最低限カバー

## 4. フロントエンド（frontend/）

### 4.1 再生エンジン（src/audio/DeckEngine.ts — Reactから独立したクラス）

- **2デッキ構成**: `deckA/deckB`、それぞれ `AudioBufferSourceNode → GainNode` → master `GainNode` → destination
- ロード: `fetch → decodeAudioData`（1曲3〜5MBなので全量デコードで問題ない）。**次曲は再生中に先読みデコード**しておく
- **クロスフェード**: equal-powerカーブ（out: `cos(t·π/2)`、in: `sin(t·π/2)`）。`setValueCurveAtTime` で実装。デフォルト8秒
- **ミックス開始タイミング**: `duration − crossfadeSeconds − 2` 秒地点を基本とし、`beats[]` があれば直近のビート時刻にスナップ。曲変更指示を受けたときは「次のビートから」即時ミックス
- **テンポナッジ**（polish枠・M4以降）: BPM差が6%以内なら入る側の `playbackRate` を相手BPMに合わせ、フェード完了後10秒かけて1.0へ戻す
- イベント発火: `trackStarted / transitionScheduled / tick(position)` → React側はこれを購読して描画するだけ。**AudioContextはユーザー操作（Startボタン）で resume する**（ブラウザの自動再生制限対策）

### 4.2 UI（ダークテーマ、DJブース風）

レイアウト（デスクトップ想定・1画面固定）:

```text
┌────────────┬──────────────────────┐
│            │  Now Playing デッキ      │
│  チャット    │  （曲名/BPM/key/energy    │
│  パネル      │   バッジ + 進行バー +      │
│            │   クロスフェード中の表示）    │
│  (DJの相槌・ ├──────────────────────┤
│  選曲理由も  │  Up Next タイムライン       │
│  ここに出る)  │  （プラン3〜5曲をカード表示、  │
│            │   各カードに選曲理由）        │
└────────────┴──────────────────────┘
```

- **チャットパネル**: 入力欄＋履歴。DJ側の発言は `intent.comment`（相槌）と `selection.reason`（遷移説明）。ユーザー発言→応答までの間はタイピングインジケータ
- **Now Playing**: 曲メタ＋進行バー。クロスフェード中は両曲を重ねて表示し、フェードの進行が見える（動画映えポイント）
- **Up Next**: `plan` のカード列。再プランで並びが変わるときはアニメーションで入れ替え（動画映えポイント）
- **候補デバッグ表示**（M2）: top-5とスコア内訳を出す開発用パネル。トグルで隠せるようにして、動画では「技術説明カット」でだけ見せる

### 4.3 セッション制御（src/state/session.ts）

- 状態: `SessionContext` ＋ 現在の `plan` キュー。Reactは useReducer か zustand（どちらでも、Codexに任せる）
- **自動継続ループ**: プランの残りが2曲を切ったら `POST /api/intent (message: null)` でプランを延長
- ユーザー入力時: `/api/intent` → 応答の `selection` で「次のビートからミックス」をDeckEngineに指示、`plan` を差し替え
- 指名リクエストで曲がカタログに無い場合: DJが「持ってない」旨をチャットで返す（GPT①の `comment` で処理）

## 5. エラー処理（デモ経路のみ）

- `/api/intent` 失敗: チャットにDJの謝罪メッセージ＋現在のプランで再生継続（音は止めない）
- mp3のデコード失敗: その曲をスキップしてプランの次へ、チャットに一言
- `OPENAI_API_KEY` 未設定: サーバー起動時に警告、`/api/intent` は機械選曲フォールバックで動作（＝キー無しでも音は出る → 審査員体験の保険）

## 6. マイルストーンと完了条件

| M | 内容 | 完了条件（これが通ったら録画してコミット） |
|---|---|---|
| M1 | モノレポ雛形＋配信＋再生＋クロスフェード | 手書きcatalog.json＋仮音源3曲で、Nextボタンにより8秒equal-powerクロスフェードが鳴る |
| M2 | スコアリング＋候補表示 | vitest緑。UIにtop-5とスコア内訳が出て、Nextが最高スコア曲へ遷移する |
| M3 | エージェント接続 | チャットで「もっと盛り上げて」→ DJIntent解釈 → 選曲 → ビート頭で自動クロスフェード → 理由がチャットに出る。指名リクエストも通る |
| M4 | セットプラン＋自動継続 | Up Nextに3〜5曲＋理由が並び、放置でも途切れず流れ続け、新しい指示でプランが組み替わる |
| polish | テンポナッジ・フェード中の重ね表示・UI磨き | 動画の絵コンテ（提出計画.md §2）が全カット撮れる |

## 7. 作らないもの（本体スコープ外）

- ストリーミング連携の実装（`MusicSource` の継ぎ目のみ）/ ChordMini高度解析 / エフェクト・スクラッチ / ユーザーアカウント / モバイル対応 / ホスティング（ストレッチ扱い）
