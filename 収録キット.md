# Vibraxis 収録キット — TTS台本 & ショットリスト

`動画ストーリー.md`（正本の絵コンテ）に対する**撮影実務レイヤー**。
実UI（`frontend/src/components/AutoDj.tsx` の自律コンダクター）に完全準拠。
これを見ながら「①ナレーションをTTSに読ませる → ②画面を撮る → ③並べる」で完成する。

- 尺: 3分（180秒）以内。ナレーション実測 約340語（≈130秒）→ 音楽の"間"に余裕あり。
- 音声: 英語必須（AI音声可）。
- 映すUI: **AUTO-DJ パネル（自律コンダクター）**。下部の手動 DJ AGENT パネルはフレーム外へ（自律の物語に集中）。

---

## パートA — TTS用ナレーション最終稿（英語・そのまま流せる）

各ブロックを個別にTTSへ。太字の語は軽く強調してもよい。`—` は短い間、`…` はやや長い間。

### Cut 1 — 0:00–0:15 ｜問題提起（24語）
> Every party has this moment: the playlist keeps playing — but the room has moved on. Shuffle can't read the room. A DJ can.

### Cut 2 — 0:15–0:35 ｜楽器の紹介（48語）
> This is Vibraxis — an AI club DJ. Under the hood is a real two-deck instrument: beat grids, key analysis, an equal-power crossfader, a limiter so it can never clip. The contract that controls it is a protocol we call VDAP. And the DJ playing this instrument is GPT-5.6.

### Cut 3 — 0:35–1:25 ｜魔法の瞬間＝自律ミックス＋steering（95語）★最重要
> I never press play on the next song. Watch — Vibraxis is running the set on its own. As one track nears its end, the engine scores every record in the crate: tempo distance, key compatibility on the Camelot wheel, energy direction. It picks the next one, waits for the bar line, and crossfades. Beat-matched. Key-compatible. No approval, no click from me. And I steer it with a sentence — I type, "keep it moody, then slowly pick it up." GPT-5.6 reads that into a structured plan, and the very next mix follows my words.

### Cut 4 — 1:25–1:45 ｜ブースの主権（38語）
> But here's the rule we care about most: the booth is still yours. Grab the crossfader mid-mix, and the AI's automation cancels instantly. Hit PANIC, and everything stops. The AI is the performer here — never the owner.

### Cut 5 — 1:45–2:05 ｜プロトコル/MCP/ライセンス（58語）
> Because Vibraxis is an instrument with a protocol, the DJ doesn't have to live inside the app. The same port the built-in AI plays through is designed to mount an MCP server — so tomorrow, any agent could sit at these decks. And the crate ships with fully licensed, pre-analyzed music: a legal record box an AI is allowed to mix.

### Cut 6 — 2:05–2:40 ｜Codex / protocol-first（57語）
> We built Vibraxis with OpenAI Codex, and we built it protocol-first. Codex wrote the VDAP specification — and a machine checker for it — before a single feature existed. Then the analyzer, the audio engine, the runtime: each stage designed, tested, and committed. Over four hundred tests, green. Give an AI a strong contract, and it builds like an engineer.

### Cut 7 — 2:40–3:00 ｜締め（20語）
> GPT-5.6 is the brain: natural language in, validated DJ decisions out — every transition explained. Vibraxis. Your set. Your words.

**タグライン（画面テロップ用）**: *"Your set. Your words."*

---

## パートB — ショットリスト（実UI準拠・撮影手順）

### 撮影前プリフライト（1回だけ・必ず）
1. `npm run dev` を起動 → ブラウザで `http://localhost:5173`。
2. **ENABLE AUDIO** をクリック → `AUDIO ACTIVE` になるのを確認。
3. `.env` に `OPENAI_API_KEY` 設定済み・`GPT56_MODEL=gpt-5.6-luna`（確認済み）。REQUEST欄が
   GPT-5.6ライブで動く前提。
4. **known-goodプロンプトを2〜3回リハ**して、POLICY表示が更新されることを目視確認:
   - `keep it moody, then slowly pick it up`（本番採用）
   - `pick it up — slowly`
   - `keep it mellow`
   LLMの非決定性で撮影事故らないよう、本番で使う1文はリハで固定する。
5. ウィンドウ/ズームを、**Deck A・クロスフェーダー・Deck B・AUTO-DJパネル**が同一画面に収まる
   ように調整。下の手動 DJ AGENT パネルはスクロールでフレーム外へ。
6. 画面録画ツールを準備（前後に余分なフレームを数秒残して撮る）。

### Cut 1（0:00–0:15）問題提起
- アプリ不要。テロップ「shuffle can't read the room」or 抽象映像/静止画。
- もしくはUIをアイドル状態でうっすら映す（暗め）。

### Cut 2（0:15–0:35）楽器の全景
- UI全景のワイド。Deck A/B の3バンド波形が見える状態でゆっくりズーム/パン。
- 波形にマウスを重ねてビート/ダウンビート/セクションのオーバーレイをチラ見せ（"real instrument"の裏付け）。

### Cut 3（0:35–1:25）★自律ミックス＋steering（最重要）
1. AUTO-DJパネルの **START SET** をクリック。
   → Deck A に自動でロード＆再生、バッジが **RUNNING**、NOW PLAYING に曲名/BPM/Camelot表示。
2. **自動ミックスを画に出すコツ**: Deck A の波形の**終端付近をクリックして再生ヘッドを「残り20秒くらい」へシーク**する。
   ループは残り24秒（`MIX_LEAD_SECONDS`）以内で発火するので、数秒以内に AUTO-DJ の status が
   `selecting the next track…` → `mixing into <曲名>…` と進み、**承認ゼロで拍同期クロスフェード**が走る。
   完了すると `now playing <曲名>`。→ これが "resident DJ" の証明カット。
   - シークが不安なら短い曲の自然終端まで待てば自動発火する（時間はかかる）。シーク方式が速く確実。
3. 続けて **REQUEST** 欄に `keep it moody, then slowly pick it up` と入力 → **SEND**。
   → status `reading your request…` → **POLICY 行が変化**（energy/tempo/key ＋ GPT-5.6 の rationale が
   引用符付きで表示）。これが GPT-5.6 がライブで文を読んだ証拠カット。**何も即再生されない**＝steering。
4. （任意）もう一度終端へシークして次の自動ミックスを起こし、選曲が新POLICYを反映している画を撮る。

**Cut 3 の注意（実装準拠・事故回避）**
- 自動ミックスは「アクティブデッキに解析グリッドがある」時のみ発火。START SET は解析済みの1曲目を
  ブートするのでOK。カタログは全曲解析済み。
- ミックス進行中に次のシークをしない（inactiveデッキが空くまで待つ）。`Could not honor: <理由>` が
  出たらそれは正直な拒否UI（見せてもよいが、ヒーローテイクでは避ける）。
- 曲が短いので「終端へシーク」が最も制御しやすい撮り方。

### Cut 4（1:25–1:45）override＋PANIC
1. 自動ミックスの**ramp中（クロスフェーダーが動いている最中）にクロスフェーダーのスライダーを掴んで動かす**
   → ユーザー操作が優先され、エージェントのオートメーションが即キャンセル（user authority）。
2. 続けて右上の **PANIC** ボタン → 全デッキ即停止。
   - 注意: PANIC は音を切るので、**このCutはアプリ撮影の最後に回す**か、撮影後に再度 START する。

### Cut 5（1:45–2:05）アーキテクチャ図（スライド素材）
- アプリではなく**作図スライド**を用意:
  - 2ポート（`uiPort`＝user / `agentPort`＝agent）、agentPort の先に「GPT-5.6 / (future) MCP」。
  - README/Devpostのパイプライン図（natural language ▶ GPT-5.6 ▶ DjIntent ▶ …）を流用してよい。
  - ライセンスバッジ（BGMer / `ATTRIBUTIONS.md`）を隅に。

### Cut 6（2:05–2:40）Codex / protocol-first（提出要件）
- 画面録画で以下を数秒ずつ:
  1. `git log --oneline --reverse` — 冒頭に「docs: lock VDAP step 0 contracts」等、**仕様・チェッカーが機能より先**の履歴。
  2. `npm run check` がグリーンに流れる様子（`Test Files ... passed` / `Tests ... passed`、計 **389** 件）。
  3. あれば Codex セッションのスクショ数枚。
  4. （任意）`npm run check:protocol-docs` の合格 or VDAP仕様書の一部。

### Cut 7（2:40–3:00）締め
- UIに戻り、セットが流れ続けている画。ロゴ＋リポジトリURL＋テロップ `Your set. Your words.`

---

## 時間切れ時の優先順位（ここだけは死守）
1. **Cut 3**（自律ミックス＋steering）— プロダクトの核。これ無しでは成立しない。
2. **Cut 4**（override / PANIC）— 「AIは奏者、主権は人間」の差別化。
3. **Cut 6**（Codex protocol-first）— 提出要件（Codexの使い方を音声で説明）。
4. あると強い: **Cut 5**（図）, **Cut 1**（コールドオープン）, **Cut 2**（全景）。

## BGM＝プロダクト自身の出力
動画中に流れる音楽は全編 Vibraxis の出力（＝BGMer/著者オリジナル曲）。**動画そのものがデモ音声**。
著作権音楽の混入なし（`ATTRIBUTIONS.md` 準拠）。
