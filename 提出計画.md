# 提出計画 — Vibraxis (DJ Agent) @ OpenAI Build Week

締切: 7/21 17:00 PT = **日本時間 7/22(水) 朝9:00**
トラック: **Apps for Your Life**

---

## 1. Problem / Audience（審査基準 "Potential Impact" 対策）

### 切り口: 「シャッフルはDJじゃない」— 音楽を流して過ごす全ての人が主役

- **問題**: 音楽アプリはシャッフルもプレイリストもできるが、**DJはできない**。テンポもキーも場の空気も無視してぶつ切りで曲が変わる。ホームパーティーでも作業中でも、結局誰かがキューをいじる係になる。
- **ソリューション**: 自然言語で意図を伝えると、互換性のある曲を選び、テンポを合わせてクロスフェードし、理由まで説明するパーソナルDJ。
- **フリー音源の位置づけ**: プロダクトの本質ではなく**デモ用データセット**。エンジンはカタログ非依存（catalog-agnostic）に設計し、「今日はバンドルしたロイヤリティフリーライブラリで動く（＝デモも著作権的にクリーン）、将来は同じインターフェースでストリーミングカタログに接続する」と語る。制約対応は一文で済ませ、ビジョンは将来展望として堂々と言う。
- **設計への反映**: 曲の取得・メタデータ参照を `MusicSource` 的なインターフェースに切り出しておく。README の Future Work にストリーミング接続を書くとき、「口だけ」でなくアーキテクチャで裏付けられる。

### 提出文ドラフト（英語・叩き台）

> **Vibraxis** is an AI club DJ that turns natural-language intent into a continuously adapting music set.
>
> Your music app can shuffle, but it can't DJ. Playlists are static, and shuffle ignores tempo, key, and the energy in the room. Whether you're hosting friends, deep in work, or winding down, someone always ends up fiddling with the queue.
>
> Tell Vibraxis "keep it mellow while we eat, then slowly pick it up" — it structures your intent, selects key- and tempo-compatible tracks, crossfades live, and explains every transition it makes.
>
> The DJ engine is catalog-agnostic. For this demo it runs on a bundled royalty-free library (analyzed for BPM, key, energy, and mood), and the same interface is designed to plug into streaming catalogs next.

---

## 2. デモ動画 絵コンテ（3分・英語ナレーション・著作権フリー音源のみ）

原則: **この絵コンテに映る機能しか作らない**。

| 時間 | 画面 | ナレーション（要旨） |
|---|---|---|
| 0:00–0:20 | 問題提起。静止画/テキストでOK | "Your music app can shuffle — but it can't read the room. Playlists don't adapt. Real DJs do. Meet Vibraxis." |
| 0:20–0:40 | Vibraxis起動。すでに1曲目が再生中。UI全景（チャット欄＋現在曲＋セットプラン） | "This is Vibraxis — an AI DJ you talk to in plain language." |
| 0:40–1:30 | **魔法の瞬間**: "give me something more energetic" と入力 → エージェントが候補をスコアリング → BPM/キー互換を表示 → **ライブでクロスフェード** → 選曲理由をエージェントが説明 | "Watch: it picks a key-compatible track, matches the tempo, crossfades — and tells you exactly why." |
| 1:30–1:55 | 指名リクエスト: "play track X by Y" → セットプラン（3〜5曲先）が組み替わる様子 | "You can request specific tracks, and it re-plans the whole set around them." |
| 1:55–2:20 | GPT-5.6の役割: チャット入力→構造化されたDJ意図(JSON)→選曲→遷移説明、の流れを図か画面で | "GPT-5.6 is the brain: it parses intent, plans the set, and narrates every transition." |
| 2:20–2:50 | **Codex使用の説明**（提出要件）: Codexセッションの画面キャプチャ数枚＋何を加速したか | "I built Vibraxis with Codex — it scaffolded the audio engine, iterated on the scoring logic with me, and ..."（実際の開発中に具体例をメモしておく） |
| 2:50–3:00 | リポジトリURL・締めの一言 | "Vibraxis. Your set, your words." |

### 撮影メモ
- 実装期間中、**動いた瞬間のスクリーン録画を毎日残す**（Codex画面も含めて）。2:20以降の素材になる。
- ナレーションはAI音声でOK（FAQ明記）。原稿を書いて TTS に読ませるのが楽。
- BGMとして流れる曲は当然すべて自前カタログ（=ロイヤリティフリー）の曲。

---

## 3. 曲DB用のロイヤリティフリー音源

### 推奨: CC-BY の曲で統一する

リポジトリに音源を同梱して公開するなら、**再配布が許されるライセンス**である必要がある。CC-BY（表示のみ必須）が最も安全。

| ソース | ライセンス | 備考 |
|---|---|---|
| **Free Music Archive** (freemusicarchive.org) | 曲ごとに異なる。**CC-BYでフィルタして使う** | ジャンル豊富。NC(非営利)・ND(改変禁止)の曲は避ける — 賞金付きハッカソン＋クロスフェード(改変)なので |
| **ccMixter** (ccmixter.org) | CC-BY多数 | エレクトロニック系が充実。DJ向き |
| **Incompetech** (Kevin MacLeod) | CC-BY | 曲数多い。ジャンルタグ・テンポ検索あり |
| Pixabay Music | 独自ライセンス | 利用は自由だが**素材そのものの再配布が不可** → repoに同梱できない。使うならダウンロードスクリプト方式 |

### 実務手順
1. 2〜3ジャンル（例: house / lo-fi / drum'n'bass）から **15〜25曲**、BPMがなだらかに分布するように選ぶ（例: 85〜128）
2. BPM・キーは librosa か Essentia で一括解析 → `catalog.json` に BPM / key / energy / genre / mood / ライセンス情報を格納
3. キー互換は **Camelot Wheel**（隣接±1 or 同番号のメジャー/マイナー）でスコアリング
4. リポジトリに `ATTRIBUTIONS.md` を置き、全曲の「タイトル / アーティスト / 出典URL / ライセンス」を列挙 → 審査要件の "relevant licensing" を満たし、プロダクトの信頼性アピールにもなる

---

## 4. 技術スタック & アーキテクチャ（確定）

**TypeScriptで統一したローカルWebアプリ**（`npm install && npm start` → ブラウザで `localhost:8080` を開く）。将来のサーバー展開（ストリーミング連携）を見据えてNode/TSに一本化。

| レイヤー | 技術 | 役割 |
|---|---|---|
| フロント | React + Vite + TypeScript | チャットUI / Now Playingデッキ / セットプラン可視化 |
| 音声再生 | Web Audio API | 2つの `GainNode` によるクロスフェード、再生位置管理 |
| バックエンド | Node.js + TypeScript + **Hono** | フロントのビルド・`tracks/`・`catalog.json` の配信 / GPT-5.6プロキシ |
| LLM | GPT-5.6（Structured Outputs） | 自然言語→DJ意図JSON、セットプラン、遷移説明の生成 |
| 前処理 | 単発スクリプト（librosa等、Codexに書かせる） | 音源フォルダ → BPM/キー/energy解析 → `catalog.json` 生成（結果をコミット） |

### 設計の要点

- **Honoを選ぶ理由**: 同じコードがNodeでもCloudflare Workersでもほぼそのまま動く → ストレッチゴールの「ホスト版デモ」が別実装でなくデプロイ作業になる
- **型をフロント/バックで共有**: `catalog.json` のスキーマ、DJ意図JSON、セットプランの型を共通パッケージ（`shared/`）に置く。TS統一の最大のメリット
- **`MusicSource` インターフェース（TS）**: `LocalFolderSource` を今回実装。将来 `StreamingSource` に差し替えられる形にして、Future Workの裏付けにする
- **APIキー**: 審査員が自分の `OPENAI_API_KEY` を環境変数で渡す。READMEに明記
- **API面**: `GET /api/catalog` / `GET /tracks/{file}` / `POST /api/intent`（現在曲・履歴・チャット入力 → GPT-5.6 → 構造化されたDJ指示）
- **スコアリング**（BPM近接 + Camelot Wheel互換 + energy方向）は決定的なロジックとしてTSで実装し、GPT-5.6は「意図の解釈」と「候補からの選択・説明」を担当 — LLMに全部丸投げしない構図のほうが動作が安定するし、審査でも技術的に見栄えする

### 前処理: analyze-tool（Python CLI・確定）

- Python 3.11 + librosa の独立CLI（`analyze-tool/`）。npm scriptsから起動、出力は `data/analysis/*.json` → `catalog.json`。本体実行時にPythonは起動しない
- **タイムボックス: 半日**。審査員は実行しないツールなので「自分のPCで20曲に正しい値が出る」がゴール
- BPMの倍/半分誤検出対策（70〜180クランプ＋耳で確認）と、**手動オーバーライド機構**（`overrides.json` が解析結果に勝つ）を入れる。キー推定の誤りとは戦わず上書きで進む
- **ChordMini（コード認識・downbeat）はスコープ外**。`providers/` の継ぎ目だけ残してREADMEのFuture Work行き。madmom+Docker+Windowsは沼で、審査に加点しない

### 見送り（ストレッチゴール）

- Cloudflareでのホスト版デモ（Hono移植 + R2に音源）— 時間が余ったら。GitHub Pagesは静的のみでAPIキーを隠せないため不採用
- `bun build --compile` によるシングルバイナリ配布 — 「1ファイルで配る」体験が欲しくなったら
- ChordMiniプロバイダー（高度解析）

### ゴール定義と品質の傾斜配分

**唯一のゴール: 7/21朝に「3分動画が撮れる状態」**。M3まで完動＋できればM4、審査員が3コマンド以内で起動できる。迷ったら「**それは3分の動画に映るか？**」で判断。映らないものはFuture Work。

| 部分 | 品質バー | 備考 |
|---|---|---|
| 再生・クロスフェード | 最高 | equal-power、ビート頭でミックス開始。音の乱れは動画で即バレる |
| 意図→選曲→説明ループ | 高 | 差別化点。説明文の質はプロンプトを磨く時間を確保 |
| UI | 動画映え基準 | ダーク＋DJデッキ風の一貫した見た目 > 機能網羅 |
| スコアリング | 中〜高 | 純関数＋vitest。テストの存在が審査で語れる |
| analyze-tool | 動けばいい | 手動オーバーライドで精度問題を回避 |
| エラー処理 | デモ経路のみ | GPT失敗時に優雅なメッセージが出れば十分 |
| README＋動画 | 最高 | 審査員は動画と説明文だけで判断してよいルール |

## 5. スケジュール（残り6日）

| 日付 | やること |
|---|---|
| 7/16(木) | 提出文・絵コンテ確定、音源選定開始、技術スタック決め |
| 7/17(金) | 曲DB構築（DL＋解析＋catalog.json）、Codexで開発開始（再生＋クロスフェード） |
| 7/18(土)–7/20(月) | Codexで実装（意図構造化→スコアリング→遷移説明→プラン可視化の順）。**毎日録画** |
| 7/21(火) | コード凍結。動画編集・ナレーション・README(英語)・提出フォーム |
| 7/22(水) 朝9:00 | 締切 |

## 6. 提出チェックリスト

- [x] Devpost登録
- [x] Codexクレジット申請
- [ ] 動くプロジェクト（Codex + GPT-5.6）
- [ ] カテゴリ: Apps for Your Life
- [ ] 説明文（英語）
- [ ] デモ動画: ≤3分 / YouTube公開 / 英語ナレーション / Codex+GPT-5.6の使い方を音声で説明 / 著作権音楽なし
- [ ] リポジトリURL（公開 + ライセンス明記、または testing@devpost.com と build-week-event@openai.com に共有）
- [ ] README（英語）: セットアップ / サンプルデータ / Codexがどこを加速したか・意思決定
- [ ] `ATTRIBUTIONS.md`（全曲のライセンス表記）
- [ ] `/feedback` セッションID（コア開発したCodexスレッドで取得）
