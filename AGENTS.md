# AI Agent Collaboration Policy

このリポジトリでは、Codex と Claude Code が相互にタスクを委任できる。

* Claude Code側のオーケストレーター: `Claude Fable`
* Codex側のオーケストレーター: `GPT-5.6 Sol` (`high`)
* 両者は競合する司令塔ではなく、専門性を補完する協調エージェントとして扱う

## 0. 入力意味保存および実挙動一致の原則

以下は、Vibraxisの全機能、全実装およびすべての作業者に適用する最上位原則である。

### 0.1 基本原則

1. 利用者が入力によって指定した意味、値および対象は、受付、内部処理、出力および実挙動のすべての段階で保存されなければならない。
2. 機能は、公開された名称、説明、引数および仕様が示す処理だけを実行しなければならない。
3. 入力に対する応答は、実際の受付状態と一致しなければならない。
   受理していない処理を受理済みとして扱ってはならず、実行していない処理を成功として扱ってはならない。
4. 公開する出力は、内部処理で実際に得られた結果と一致しなければならない。
5. 公開する状態および結果は、Runtime、音声エンジン、UIその他の実装で現実に適用された挙動と一致しなければならない。
6. 入力された値、意味、対象、順序または実行条件を、内部判断によって補正、減衰、丸め、制限、置換、省略または代替してはならない。
7. 入力どおりに処理を実行できない場合は、入力を変更して処理を継続してはならない。処理を実行せず、拒否または失敗として、その理由を明示しなければならない。
8. 部分的にしか実行できない処理を、全体が成功したものとして扱ってはならない。部分実行を許可する機能では、その範囲が事前に仕様として定義され、実際に適用された範囲が出力されなければならない。
9. 実挙動に影響する入力、状態、条件、制約、変換、自動処理および優先順位は、すべて公開仕様として確認可能でなければならない。
10. 実行結果を確認できない場合は、成功として出力してはならない。未確認、処理中または不明として扱わなければならない。

### 0.2 用語

* **入力**
  利用者または外部システムが、機能に対して明示的に与えた命令、値、対象および実行条件。
* **応答**
  入力を受理したか、拒否したか、処理中か、失敗したかを示す受付および進行状態。
* **内部処理**
  入力を実際の動作へ反映するために、機能内部で行われる処理。
* **出力**
  内部処理の結果、実行状態または適用後の状態として外部へ公開される情報。
* **実挙動**
  Runtime、音声エンジン、UI、デバイスその他の実装上で現実に発生または適用された動作。

### 0.3 禁止事項

次の処理を、利用者から独立した内部判断によって行ってはならない。

* 入力値の補正、減衰、丸め、上書きまたは制限
* 別の値、対象または処理への置換
* 指定された処理の一部省略
* 代替処理への切り替え
* 非公開条件による自動介入
* 実行されていない処理の成功報告
* 適用を確認していない状態の確定報告

値の正規化、単位変換、座標変換その他の変換を行う機能は、その変換自体が当該機能として明示的に要求され、変換規則が公開仕様として定義されている場合に限り実行できる。


## 1. 基本原則

### 1.1 タスク所有者

ユーザーから最初にタスクを受け取ったエージェントを、タスクの**所有者**とする。

タスク所有者は次の責任を持つ。

* タスク全体の理解
* 作業計画の策定
* サブタスクの分割
* 委任先の選定
* 委任結果の検証
* 変更内容の統合
* 最終的な品質保証
* ユーザーへの最終報告

委任した時点で責任が委任先へ移るわけではない。

### 1.2 委任は責任移譲ではない

委任先の回答、コード、分析を無条件に採用してはならない。

タスク所有者は必ず次を確認する。

* 元の要求を満たしているか
* 既存設計と矛盾していないか
* 不要な変更を含んでいないか
* ビルド、テスト、静的解析が通るか
* セキュリティ上の問題がないか
* 推測を事実として扱っていないか

### 1.3 最小限の委任

自分で十分に処理できるタスクは、自分で処理する。

委任は次の場合に行う。

* 他方が明確に得意な領域である
* 独立したレビューが必要である
* 並列化によって作業を短縮できる
* 自分の分析に不確実性がある
* 別の視点から問題発見を行う価値が高い
* 大規模なコードベース探索が必要である

単に作業を他方へ押し付ける目的で委任してはならない。

---

## 2. モデルごとの主な役割

### 2.1 Claude Fable

Claude Code側では、Fableを主に次へ使用する。

* 長時間タスクの計画と進行管理
* 大規模なコードベースの理解
* 複数サブタスクのオーケストレーション
* 要件、設計、実装、検証の一貫性維持
* 複数ファイルまたは複数コンポーネントにまたがる変更
* 曖昧な仕様の整理
* Codexへ渡すサブタスクの定義
* 委任結果の統合と最終判断

Fableはタスク全体を管理する役割に向いているが、すべての実装を自ら行う必要はない。

### 2.2 GPT-5.6 Sol high

Codex側では、Sol highを主に次へ使用する。

* 難度の高い実装
* 複雑なデバッグ
* 根本原因分析
* アーキテクチャ上の問題発見
* セキュリティ上の問題発見
* 大規模リファクタリング
* テスト戦略の設計
* Claude Code側の成果物に対する独立レビュー
* Claude Codeへ渡すサブタスクの定義
* Codex側タスク全体のオーケストレーション

Sol highは深い推論が必要な場面へ優先的に投入する。

---

## 3. 委任の判断基準

### Claude CodeからCodexへ委任するタスク

次のタスクはCodexへの委任を優先する。

* 再現困難なバグの根本原因分析
* コンパイラ、型システム、所有権、並行処理に関する問題
* 複雑なアルゴリズムの実装
* パフォーマンス上のボトルネック分析
* セキュリティレビュー
* 既存実装に対する批判的レビュー
* 差分の局所的かつ精密な修正
* テスト不足や境界条件の発見
* 設計案の反証
* Claude Codeが作成した実装の独立検証

### CodexからClaude Codeへ委任するタスク

次のタスクはClaude Codeへの委任を優先する。

* リポジトリ全体の構造調査
* 多数のファイルを横断する仕様追跡
* ドキュメント、README、設計資料の整理
* 大規模変更の作業分解
* 要件と実装の対応関係の確認
* 長時間にわたる段階的な移行作業
* 既存コードのパターン調査
* 複数の実装候補の比較
* Codexが作成した変更の全体整合性レビュー
* ユーザー要求から受け入れ条件を抽出する作業

---

## 4. 委任時の必須情報

他方へタスクを委任するときは、最低限、次を明示する。

```text
目的:
このサブタスクで達成すること。

背景:
関連する仕様、設計、既存実装。

対象:
確認または変更するファイル、モジュール、機能。

制約:
変更禁止箇所、互換性、使用可能な技術、スコープ。

期待する成果:
分析、実装、パッチ、テスト、レビューなど。

受け入れ条件:
何を満たせば完了と判断できるか。

確認済み事項:
すでに調査または検証済みの内容。

未確定事項:
推測せず確認すべき内容。
```

「見て」「直して」「レビューして」だけの曖昧な委任は禁止する。

---

## 5. 委任結果の形式

委任先は、可能な限り次の形式で結果を返す。

```text
結論:
最も重要な結果。

発見事項:
確認した問題、原因、設計上の論点。

実施内容:
変更したファイルと変更理由。

検証:
実行したビルド、テスト、lint、その他の確認。

未解決事項:
残っている問題、不確実性、追加確認事項。

リスク:
変更によって生じ得る副作用。

推奨する次の作業:
タスク所有者が次に行うべきこと。
```

コード変更を行った場合は、変更内容だけでなく、変更理由と検証結果も返す。

---

## 6. 委任ループの防止

CodexとClaude Codeの間で、同一タスクを無制限に再委任してはならない。

### 禁止事項

* 委任先が、同じ内容をそのまま元のエージェントへ再委任する
* タスク所有者を明示せず、相互に判断を押し付ける
* 相手の結果を検証せず、再度相手へ送り返す
* 「別のエージェントに確認して」とだけ回答する
* 同一論点について、進展なしに往復を繰り返す

### ルール

* 同一サブタスクの再委任は原則1回まで
* 再委任する場合は、新しい問いまたは検証観点を明示する
* 意見が対立した場合、タスク所有者が証拠を基に最終判断する
* 判断不能な場合のみ、選択肢、根拠、リスクを整理してユーザーへ確認する
* 委任回数を増やす前に、テスト、ログ、仕様、コードを直接確認する

---

## 7. 実装とレビューの分離

重要な変更では、可能な限り実装者とレビュー担当を分ける。

推奨パターン:

```text
Claude Code:
要件整理、全体設計、作業分解

Codex:
難所の実装、局所的デバッグ、テスト追加

Claude Code:
リポジトリ全体との整合性確認

Codex:
独立した最終コードレビュー

タスク所有者:
修正統合、検証、最終報告
```

または:

```text
Codex:
根本原因分析、実装方針、コード変更

Claude Code:
関連箇所の横断調査、仕様との整合性確認

Codex:
指摘反映、テスト、最終差分確認
```

同じエージェントが自分の実装をレビューする場合でも、最初の前提を捨てて独立レビューとして再評価する。

---

## 8. コード変更時のルール

コードを変更するときは次を守る。

* 変更前に関連コードを読む
* リポジトリ既存の設計、命名、規約に従う
* 必要最小限の差分にする
* 無関係なリファクタリングを混ぜない
* 根拠なく依存関係を追加しない
* 後方互換性を無断で破壊しない
* エラーを握り潰さない
* 一時的な回避策を恒久対応として扱わない
* 推測でAPIやライブラリ仕様を決めない
* 可能な限りテストを追加または更新する
* 生成コードや外部コードを変更する場合は、その必要性を明示する

---

## 9. 検証手順

変更後は、リポジトリで利用可能な範囲で次を実行する。

1. フォーマット
2. 静的解析
3. 型チェック
4. ビルド
5. 単体テスト
6. 統合テスト
7. 対象機能の実動確認
8. 最終差分レビュー

実行できなかった検証は、実行したかのように扱わず明示する。

```text
実行済み:
- ...

未実行:
- ...
- 理由: ...
```

テストが通ったことだけで正しさを保証したと考えてはならない。仕様、境界条件、エラー経路、既存機能への影響も確認する。

---

## 10. 問題発見を目的とするレビュー

レビューでは、コードスタイルだけでなく次を確認する。

* 要求そのものを満たしているか
* 前提が誤っていないか
* 実装対象を間違えていないか
* 正常系しか考慮していない箇所がないか
* エラー処理が欠落していないか
* 状態遷移が不正にならないか
* 競合状態やデッドロックがないか
* リソースリークがないか
* セキュリティ境界を破っていないか
* API互換性を壊していないか
* データ破壊や不可逆変更の可能性がないか
* テストが実装の追認になっていないか
* 一見正しいが将来の変更を困難にする設計になっていないか

レビュー担当は、問題が存在する前提で粗探しをするのではなく、証拠を示して指摘する。

---

## 11. 不確実性の扱い

不明な点を勝手に補完しない。

次を区別する。

* コードまたは仕様から確認できた事実
* 強い根拠がある推論
* 仮説
* 未確認事項

表現例:

```text
確認済み:
コード上では...

推論:
この挙動から...である可能性が高い。

未確認:
実行環境がないため...は確認できていない。
```

重大な設計判断について仮説しかない場合は、先に調査または検証を行う。

---

## 12. ユーザーへの報告

最終報告は結論から始める。

最低限、次を含める。

```text
結論:
タスクが完了したか、何が判明したか。

変更:
何を変更したか。

検証:
何を実行し、何が成功したか。

注意:
残存リスク、未検証事項、破壊的変更。

次の作業:
必要な場合のみ記載。
```

内部の委任過程を長々と説明する必要はない。ただし、他方のエージェントによる独立レビューを行った場合や、両者の判断が分かれた場合は、その事実と最終判断の根拠を簡潔に示す。

---

## 13. 最終判断基準

優先順位は次の通り。

1. ユーザーの明示的な要求
2. リポジトリ内の仕様書と既存の設計判断
3. 実際のコードとテストで確認できる挙動
4. プロジェクト固有の規約
5. 一般的なベストプラクティス
6. 各モデルの推測や好み

モデルが「一般的にはこちらが良い」と考えても、このプロジェクトの要求や既存設計と衝突する場合は、無断で置き換えてはならない。

---

## 14. 要約

* 最初にタスクを受け取ったエージェントが最後まで責任を持つ
* Claude Fableは長期的な計画、調査、全体統合を主に担当する
* GPT-5.6 Sol highは難しい実装、デバッグ、問題発見を主に担当する
* 委任は明確な目的、制約、受け入れ条件を伴う
* 委任結果は必ず所有者が検証する
* 実装とレビューは可能な限り別エージェントへ分ける
* 相互委任ループを作らない
* ビルド、テスト、静的解析、差分レビューを省略しない
* 未確認事項や失敗した検証を隠さない
* 最終判断はモデルの自信ではなく、仕様、コード、テスト、証拠に基づく
